# Was die Testsuite unter `tests/` braucht.
#
#     pip install -r requirements-dev.txt
#     pytest
#
# Gepinnt aus demselben Grund wie ruff: Ein Upstream-Release soll die CI an
# unberührtem Code nicht rot färben, zu einem Zeitpunkt, den niemand gewählt
# hat. Bewusst anheben.
#
# ruff steht bewusst NICHT hier. Die Version wird in
# .github/workflows/ci.yml gepinnt und mit .pre-commit-config.yaml abgeglichen
# (Prüfung 12); ein dritter Ort für dieselbe Zahl wäre ein dritter Ort zum
# Auseinanderlaufen.
pytest==9.1.1
