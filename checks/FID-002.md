---
id: FID-002
title: "Recall-Ground-Truth: Referenzqueries gegen die offizielle Oberfläche"
category: FID
severity: high
applies_when: 'tools_make_external_requests == true'
pdf_ref: "Custom (Portfolio-Fundstück termdat-mcp#11)"
evidence_required: 3
---

# FID-002 — Recall-Ground-Truth

## Description

Ein MCP-Server ist eine zweite Oberfläche auf einen Bestand, für den es bereits eine erste gibt: das offizielle Web-UI der Datenquelle. Diese erste Oberfläche ist die einzige verfügbare Ground Truth. Wer sie nicht abfragt, hat kein Mittel festzustellen, ob der Server liefert, was die Quelle hat.

Der Unterschied zu einem gewöhnlichen Integrationstest ist grundsätzlich: Ein Test gegen die API prüft, ob der Server die API korrekt bedient. Ein Recall-Vergleich gegen das Web-UI prüft, ob die Bedienung **vollständig** ist. Der zweite Fall ist der, der `termdat-mcp` durchgerutscht ist — 33 grüne Offline-Tests und 3 grüne Live-Tests, während zwei Prozent des Bestands durchsucht wurden. Kein Mock kann diese Klasse finden, weil er genau die Annahme abbildet, die falsch ist.

Bemerkenswert am Portfolio-Fall: Der `mcp-data-source-probe`-Skill enthielt die Regel bereits (Abschnitt 1.4, «Reality-Check gegen Homepage-Zahlen»). Sie wurde auf die Listen-Endpoints angewandt — 140 Collections, 23 Classifications, beide korrekt — und nie auf den Such-Endpoint. Nicht die Regel fehlte, sondern ihre Reichweite. Dieser Check zieht die Regel auf Query-Endpoints, weil dort der Recall entsteht.

Der Vergleich ist zusätzlich das einzige Frühwarnsystem für **Upstream-Default-Änderungen**. Bei einem Portfolio aus 30+ Servern gegen Behörden-APIs ist die Frage nicht, ob eine Quelle ihre Defaults ändert, sondern welche zuerst.

## Verification

### Modus 1: documentation_check (Referenzquery-Set vorhanden)

Der Server dokumentiert ein Set von 3–5 Referenzqueries mit den zum Prüfzeitpunkt gemessenen Trefferzahlen beider Oberflächen:

```bash
grep -rn -i "ground.truth\|recall\|Referenzquer\|Web-UI\|Reality-Check" README.md README.de.md CHANGELOG.md
```

**Pass-Pattern** (im README unter «Known Limitations» oder als Probe-Befund):

| Referenzquery | Web-UI | MCP-Tool | Delta | Erklärung |
|---|---:|---:|---:|---|
| `Quellensteuer` | 12 | 7 | −5 | UI zählt Benennungen, Tool zählt Entries |
| `Pensionskasse` | 25 (gekürzt) | 28 | +3 | UI-Anzeige gekürzt |
| `Bundeskanzlei` | 4 | 4 | 0 | — |

Ein Delta ist zulässig — aber **nur erklärt**. «Weiss ich nicht» ist ein Fund, kein Ergebnis.

### Modus 2: runtime_test (Recall-Regressionstest)

Das Referenzquery-Set existiert zusätzlich als Live-Test mit Mindest-Trefferzahlen. Nicht exakte Zahlen — die schwanken mit dem Bestand —, sondern Untergrenzen, die einen Scope-Kollaps sicher fangen:

```python
@pytest.mark.live
async def test_recall_floor():
    """Recall-Canary: fängt Scope-Regressionen und Upstream-Default-Änderungen."""
    client = TermdatClient()
    for term, floor in [("Pensionskasse", 10), ("Quellensteuer", 1), ("Bundeskanzlei", 1)]:
        entries, _ = await client.search(term, "DE", max_results=100)
        assert len(entries) >= floor, f"{term}: {len(entries)} < {floor} — Scope geschrumpft?"
    await client.aclose()
```

Untergrenzen grosszügig unter dem gemessenen Ist-Wert wählen (Faustregel: die Hälfte). Der Test soll einen Kollaps von 21 auf 1 fangen, nicht bei jeder Bestandspflege rot werden.

## Pass Criteria

- [ ] 3–5 Referenzqueries definiert, die den typischen Nutzungsfall abdecken (nicht nur den Anchor-Demo-Query)
- [ ] Trefferzahlen beider Oberflächen zum selben Zeitpunkt gemessen und dokumentiert
- [ ] Jedes Delta ist erklärt — Zählweise, Kürzung, Sprachraum, Feldabdeckung
- [ ] Recall-Regressionstest mit Untergrenzen existiert und läuft unter `@pytest.mark.live`
- [ ] Der Vergleich deckt **Query-/Such-Endpoints** ab, nicht nur Listen-Endpoints
- [ ] Bei Servern ohne offizielles Web-UI: dokumentierte Ersatz-Ground-Truth (Bulk-Dump-Zeilenzahl, veröffentlichte Bestandszahlen)

## Common Failures

| Anti-Pattern | Risiko |
|---|---|
| Reality-Check nur gegen Listen-Endpoints | Recall-Bugs in der Suche bleiben unsichtbar |
| Nur Mock-Tests, keine Ground Truth | Der Mock bestätigt die falsche Annahme |
| Exakte Trefferzahlen als Assertion | Test wird bei jeder Bestandspflege rot, wird abgeschaltet, fängt dann nichts mehr |
| Delta gemessen, aber nicht erklärt | «Wahrscheinlich Zählweise» ist eine Vermutung, kein Befund |
| Nur der Anchor-Demo-Query getestet | Genau der Query funktioniert immer — er wurde beim Bau optimiert |
| Ground Truth einmalig zum Release, nie wieder | Upstream-Default-Änderungen bleiben unbemerkt |

## Remediation

1. 3–5 Referenzqueries wählen: je einer mit vielen Treffern, einer mit wenigen, einer mit Kompositum/Sonderzeichen.
2. Beide Oberflächen am selben Tag abfragen, Zahlen in eine Tabelle.
3. Jedes Delta auflösen, bis die Erklärung belastbar ist. Ist sie es nicht, ist der Befund offen und gehört ins README.
4. Untergrenzen als `@pytest.mark.live`-Test festschreiben.
5. Optional in den `mcp-continuous-auditor` als Recall-Canary übernehmen, damit der Vergleich wiederkehrend läuft statt einmalig.

## Effort

S — Pro Server 2–3 Stunden für Erstmessung und Test. Der wiederkehrende Lauf ist Automatisierung, kein Aufwand.

## References

- Portfolio-Fundstück: [malkreide/termdat-mcp#11](https://github.com/malkreide/termdat-mcp/issues/11)
- FID-001 — Scope-Defaults (die häufigste Ursache eines Recall-Deltas)
- `mcp-data-source-probe`-Skill, Abschnitt 1.4 «Reality-Check gegen Homepage-Zahlen»
- OPS-001 — Test-Strategie (Recall-Tests ergänzen Happy/Retry/Timeout, ersetzen sie nicht)
