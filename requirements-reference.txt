# Laufzeit-Abhaengigkeiten der Vorlagen unter `reference/`.
#
# Nur fuer den Import-Smoke-Test in scripts/validate.sh: die Vorlagen werden
# tatsaechlich importiert, nicht bloss kompiliert. Ohne diese Pakete schlaegt
# der Check fehl — bewusst mit Meldung statt mit stillem Ueberspringen, sonst
# meldete ein Lauf ohne Abhaengigkeiten «bestanden», wo «nicht gelaufen»
# richtig waere.
#
# Gepinnt aus demselben Grund wie ruff: ein Upstream-Release soll die CI an
# unberuehrtem Vorlagen-Code nicht rot faerben. Bewusst anheben.
#
# Nicht die Abhaengigkeiten der Zielserver — die pinnt jeder `*-mcp`-Server
# selbst. Hier steht nur, was noetig ist, um die Vorlagen zu laden.
pydantic==2.13.4
httpx==0.28.1

# Fuer `skills/mcp-transport-hardening/reference/patterns.py`, seit Phase 5.
#
# DIESER PIN IST DER TRAGENDE. Die Vorlage beschreibt die Oberflaeche von
# SDK 2.x (`mcp.server.mcpserver.MCPServer`, `mcp.server.transport_security`);
# `transport/12` haelt sie gegen GENAU DIESE Version. Die 1.x-Fassung
# (`mcp.server.fastmcp`) existiert in 2.0.0 nachweislich nicht mehr — das ist
# der Gegenstand von Regel 1 jenes Skills.
#
# Was der Pin NICHT merkt, gehoert dazugesagt: Verschiebt upstream die
# Oberflaeche in 2.1, bleibt `transport/12` gruen und die Vorlage veraltet
# still. Genau dafuer laeuft `.github/workflows/sdk-drift.yml` woechentlich
# gegen die jeweils NEUESTE mcp — und liest den Pin aus dieser Datei.
#
# `starlette` und `uvicorn` stehen bewusst nicht einzeln hier: Sie kommen als
# Abhaengigkeiten von `mcp` mit, und sie einzeln zu pinnen hiesse, das
# Aufloesungsergebnis des SDK zu ueberstimmen.
mcp==2.0.0
