"""Werkzeuge des Repositories — kein Teil der Vorlagen unter `reference/`.

Was hier liegt, wird nicht kopiert. `reference/patterns.py` ist das Material,
das in fremde Server wandert; `tools/` prüft dieses Repository selbst.
"""
