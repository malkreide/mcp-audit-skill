# Was die Testsuite unter `tests/` braucht — nicht die Vorlagen.
#
# Die Laufzeit-Abhängigkeiten der Vorlagen stehen in
# requirements-reference.txt; sie werden auch hier gebraucht, weil Check 3 die
# Vorlagen wirklich importiert und der Meta-Test in
# tests/test_suite_integrity.py jede Prüfung gegen das echte Repository fährt.
#
#     pip install -r requirements-dev.txt -r requirements-reference.txt
#     pytest
#
# Gepinnt aus demselben Grund wie ruff: Ein Upstream-Release soll die CI an
# unberührtem Code nicht rot färben, zu einem Zeitpunkt, den niemand gewählt
# hat. Bewusst anheben.
#
# ruff steht bewusst NICHT hier. Die Version wird in
# .github/workflows/ci.yml gepinnt und mit .pre-commit-config.yaml
# abgeglichen; ein dritter Ort für dieselbe Zahl wäre ein dritter Ort zum
# Auseinanderlaufen.
pytest==9.1.1
